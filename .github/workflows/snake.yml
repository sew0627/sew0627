name: Generate Snake Animation

on:
  schedule:
    - cron: "0 */24 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: Platane/snk@v3
        with:
          github_user_name: sew0627
          outputs: |
            assets/snake.svg

      - uses: EndBug/add-and-commit@v9
        with:
          message: "generated snake animation"
